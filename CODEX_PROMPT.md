# Codex continuation prompt

You are continuing a 3D portfolio web app (React + Three.js + Rapier).

Goals:
1. Make the car drivable with WASD, touch joystick, and gamepad.
2. Add 4 zones (architecture, characters, vehicles, products) with distinct visuals.
3. Add ambient audio (engine hum, wind) using Howler.
4. Improve performance: Draco-compressed glTF, texture atlases, LOD.
5. Add a clean HUD and project modal.
6. Keep 60 FPS on mid-range mobile.
7. Build passes with `npm run build`.

Start by reading the existing files, then implement the highest-impact improvements first.
